using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestDialoguePrompt : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //this is the only thing that needs to be added to any script:
        //use DialogueAttack / DialogueDirty / DialogueCleaning / DialogueRandom
        
    }

    // Update is called once per frame
    void Update()
    {
    }
}
